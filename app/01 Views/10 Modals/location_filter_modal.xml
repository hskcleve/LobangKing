<Page id="locationFilterPickerModal" shownModally="onShownModally">
    <GridLayout rows="auto,*" height="40%">
        <FlexboxLayout class="modalHeader">
            <Label row="0" text="Select Location Filter"/>
        </FlexboxLayout>
        <StackLayout row="1" style="padding:10%">
            <ListPicker id="listPicker" items="{{filterLocations}}"/>
             <Button class="-primary" text="Confirm" tap="confirmOnTap"/>
        </StackLayout>
       
    </GridLayout>
</Page>